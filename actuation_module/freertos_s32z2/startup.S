/*
 * SPDX-License-Identifier: BSD-3-Clause-NXP
 *
 * TODO(S32Z2 RTD, plan Task 3 Step 2): replace this stub with the
 * Cortex-R52 startup file shipped in $S32_RTD_PATH. Plan instructs
 * copying verbatim from the RTD sample (license header retained).
 *
 * Until the RTD is installed and this file is replaced, the build
 * intentionally fails here so no one accidentally flashes a kit with
 * a zero-initialized vector table.
 */

.error "actuation_module/freertos_s32z2/startup.S is an RTD placeholder. Copy the Cortex-R52 startup.S from $S32_RTD_PATH (see plan Task 3 Step 2)."
